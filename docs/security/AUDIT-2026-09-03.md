# ZaqorinCore v3.2.1 — Security Audit (PHASE 3 deep recon)

**Audit date:** 2026-09-03
**Subject:** ZaqorinCore v3.2.1 at commit `57e3af4` (HEAD of main)
**Repository:** github.com/Faris-stuck/zaqorincore (MIT, public OSS)
**Auditor:** Phase 3 deep-recon subagent (self-hunt, 5-Bot pipeline)
**Scope:** Medium+ severity findings across 15 vuln classes. No code modifications,
no commits.

## Phase 1 results (already shipped as v3.2.1)

| ID | Title | Status in v3.2.1 |
|---|---|---|
| F-001 | WebSocket auth accepted `shared_secret` in plaintext | FIXED (HMAC challenge) |
| F-002 | Secrets written to disk with default umask | FIXED (0600 perms) |
| F-003 | SOAR webhook SSRF — no host allow-list | FIXED (block 169.254/16, 127/8, etc.) |
| F-004 | `nft` exec injected user input | FIXED (structured args, validation) |

Phase 1 also surfaced 15+ medium/low items without per-finding docs. **Phase 3 closes
the documentation gap and runs the 15-class deep recon.**

## Phase 3 findings (NEW)

| ID | Title | Severity | Class |
|---|---|---|---|
| [F-005](findings/F-005-fastapi-version-mismatch.md) | FastAPI `app.version` hardcoded "3.2.0" vs pyproject "3.2.1" | Low | Info disclosure / op drift |
| [F-006](findings/F-006-stats-version-info-disclosure.md) | `/api/v1/stats` and `/api/v1/version` unauthenticated — leak version, SHA, pid, agent count | Medium | Info disclosure (CWE-200) |
| [F-007](findings/F-007-csp-permits-cdn-react.md) | CSP allows `https://esm.sh` React CDN, no SRI | Medium | Supply chain (CWE-829) |
| [F-008](findings/F-008-audit-log-in-memory-only.md) | Audit log is in-memory ring buffer, lost on restart | Medium | Insufficient logging (CWE-778) |
| [F-009](findings/F-009-websocket-no-message-size-cap.md) | `/ws/agent` has no per-frame size limit or heartbeat | Medium | Uncontrolled resource consumption (CWE-400) |
| [F-010](findings/F-010-cors-not-explicitly-disabled.md) | No explicit CORS policy; relies on Starlette default | Medium | Permissive cross-domain (CWE-942) |
| [F-011](findings/F-011-loose-dependency-pinning.md) | All deps pinned with `>=` ranges; no lockfile | Medium | Supply chain (CWE-1357) |
| [F-012](findings/F-012-whoami-leaks-dev-mode.md) | `/auth/whoami` returns dev-mode state + role list | Low | Info disclosure (CWE-200) |
| [F-013](findings/F-013-ingest-endpoints-no-audit.md) | Ingest endpoints never call `audit.record()` | Low | Insufficient logging (CWE-778) |
| [F-014](findings/F-014-dependency-known-bad-check.md) | Dependency CVE scan baseline — no known-bad pins at v3.2.1 | Low | Informational |
| [F-015](findings/F-015-install-command-curl-pipe-bash.md) | `curl | bash` installer with no script signature | Low | Code-without-integrity (CWE-494) |
| [F-016](findings/F-016-csp-unsafe-inline-style.md) | CSP `style-src` allows `'unsafe-inline'` | Low | UI redress (CWE-1021) |

## Severity summary

| Severity | Findings | Class tally |
|---|---|---|
| Critical | 0 | — |
| High | 0 | — |
| Medium | 6 | F-006, F-007, F-008, F-009, F-010, F-011 |
| Low | 6 | F-005, F-012, F-013, F-014, F-015, F-016 |

## 15 vuln classes — coverage matrix

| # | Class | Finding(s) | Notes |
|---|---|---|---|
| 1 | API endpoints — auth bypass via alternate paths | CLEAN | All `/api/v1/*` routers checked. `require_role` consistent. No auth path bypass found. |
| 2 | SQL injection | CLEAN | All SQL via SQLAlchemy ORM (`sa_select`, `pg_insert`). One `text("SELECT 1")` in health.py — literal-only, safe. |
| 3 | Path traversal in static / file paths | LOW | `FileResponse(_WEBUI_DIR / "index.html")` uses package-anchored path, no user input. Build-info read uses anchored Path. **No CVE — confirmed safe.** |
| 4 | Deserialization / unsafe loading | CLEAN | No `pickle.load`, no `yaml.load` (only `tomllib.loads` — safe stdlib). |
| 5 | Subprocess / shell injection | CLEAN | No `shell=True`, no `os.system`. nft uses `exec.CommandContext` with structured args (F4 fix). |
| 6 | Timing-attack compares | CLEAN | All secret/sig comparisons use `hmac.compare_digest`. Verified in `auth.py`, `stream.py`, `ingest_cloudflare.py`, `sources.py`, `evidence.py`, `crypto.py`. |
| 7 | Rate limiting / brute force | MEDIUM | `RateLimitMiddleware` covers most routes; ingest endpoints covered. WS not covered (see F-009). |
| 8 | Audit log integrity | MEDIUM | In-memory only (see F-008). No append-only file mode anywhere. |
| 9 | WebUI-specific | MEDIUM | CSP allows CDN React (F-007), `'unsafe-inline'` style (F-016). No `dangerouslySetInnerHTML` found. |
| 10 | Dependency scan | LOW | No known-bad pins (F-014); loose pinning (F-011). `pip-audit` / `govulncheck` not installed in env. |
| 11 | Information disclosure | MEDIUM | `/stats` + `/version` unauth (F-006). `/whoami` leak (F-012). App version drift (F-005). |
| 12 | Cryptographic issues | CLEAN | `secrets` used everywhere. No `random` in security paths. `hashlib.md5`/`sha1` not found for security purposes. |
| 13 | Session/CSRF | N/A | Server is stateless — no operator session cookies. API key auth per-request. CSRF not applicable. |
| 14 | Supply chain | MEDIUM | No lockfile (F-011). All deps use ranges. No LICENSE conflict (all deps MIT-compatible). |
| 15 | WebSocket DoS | MEDIUM | No max-frame-size, no idle timeout (F-009). |

## What was NOT found (good)

* No SQL injection patterns
* No pickle / unsafe-yaml / marshal
* No `shell=True` / `os.system`
* No `hashlib.md5`/`sha1` for security purposes
* No `random.choice` / `random.randint` in crypto paths
* No `==` on tokens/hashes (all use `hmac.compare_digest`)
* No `dangerouslySetInnerHTML` in WebUI
* No `cors` middleware with permissive `allow_origins`
* No hardcoded test creds in production code paths
* No known-bad minimum versions on direct deps

## Top 3 issues (one-line answer)

1. **`/api/v1/stats` + `/api/v1/version` leak version, git SHA, pid, agent count to unauthenticated callers** (F-006)
2. **No lockfile — every dep is a `>=` range, supply-chain drift on every install** (F-011)
3. **`audit` log is in-memory only — lost on process restart, bounded to 1024 entries** (F-008)

## Methodology

* Working dir: `/home/ubuntu/zaqorincore-workspace`
* Tools used: ripgrep (`search_files`), `read_file`, `terminal` (git log, wc), no `pip-audit` / `govulncheck` available.
* No code modified, no commits made.
* Per-finding docs: `docs/security/findings/F-NNN-*.md`
* Remediation plan: `docs/security/remediation/REMEDIATION-PLAN.md`
## Disclosure

This audit was a **self-hunt** (ZaqorinCore auditing ZaqorinCore) run on 2026-09-03. No findings have been disclosed outside the project. The audit is published now as part of v3.2.1 so that operators can:

1. See what was found and fixed (F-001..F-004 → shipped as v3.2.1).
2. See what is still open (F-005..F-016 → backlog for v3.2.2 / v3.3.0).
3. Run their own security review against the same checklist.

Public disclosure of a self-hunt is unusual but consistent with the project's
operating principles: zero marketing fluff, zero non-disclosure theatre, all
findings (open and closed) published alongside the version that contains them.

---

## Round 2 (post-v3.4.0) — 2026-09-03

Internal re-hunt of code added since v3.2.0 (commits 5c93ccd..b8c8cac).
Scope: `server/src/zaqorincore_server/self_defense/`, `agents_provision.py`,
new event types (`nft.call`, `process.exec`), audit endpoint changes.

| ID | Severity | Title | Status |
|---|---|---|---|
| F-015 | Low | Install command `curl | tar` (no integrity) | **Closed in v3.4.1** (download-verify-execute) |
| F-017 | Medium | CSP report throttle keyed by document-uri not src_ip | Open (fix in v3.4.1 hotfix) |
| F-018 | Low | self_defense emit() not concurrency-safe; per-worker state | Open (documented limitation) |

### Round 2 hygiene

- Bloat: 0
- IP/cred leaks in new code: 0
- AI-jargon in new code: 0
