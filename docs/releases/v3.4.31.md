# v3.4.31

## Highlights

- **F-030 closed** - defence-in-depth. Three remaining `json.loads`
  call sites now use `safe_loads` from `utils.depth_json`.
- `grep -rn "json.loads" server/src/` now returns zero hits outside
  `utils/depth_json.py` — the entire server parses JSON through a
  depth-limited decoder.

## F-030 — Server-wide JSON depth audit

A narrow re-hunt of the entire server tree for `json.loads` after
F-029 surfaced three remaining call sites:

| File | Line | Risk class |
|------|------|------------|
| `server/src/zaqorincore_server/evidence.py` | 244 | Operator disk |
| `server/src/zaqorincore_server/api/v1/evidence.py` | 108 | Operator disk |
| `server/src/zaqorincore_server/error_envelope.py` | 211 | Upstream error body |

All three now use `safe_loads`. The change is pure defence-in-
depth — no new test file because `safe_loads` is already covered by
the F-027 / F-028 / F-029 test files.

## Install

```bash
pip install zaqorincore==3.4.31
```
